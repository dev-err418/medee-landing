import { ReactElement } from "react";

export type ContentListType = {
  title: string;
  content: string;
  Icon: ReactElement;
};

export type FounderType = {
  img: string;
  name: string;
  role: string;
  motivation: string;
  obsession: string;
  fit: string;
  super: string;
};
